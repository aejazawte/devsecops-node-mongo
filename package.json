{
    "name": "stateful-node-mongo-app",
    "version": "1.0.0",
    "description": "Sample stateful Node.js app with MongoDB for Kubernetes",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "dev": "nodemon server.js",
        "build": "mkdir -p frontend/build && echo '<h1>Build Successful!</h1>' > frontend/build/index.html && echo 'console.log(\"App Build Done\");' > frontend/build/app.js"
    },
    "dependencies": {
        "express": "^4.18.2",
        "mongoose": "^7.6.1",
        "body-parser": "^1.20.2"
    },
    "devDependencies": {
        "nodemon": "^3.0.2"
    }
}